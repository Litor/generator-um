language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-oniui' ]; then cd .. && eval "mv $currentfolder generator-oniui" && cd generator-oniui; fi
